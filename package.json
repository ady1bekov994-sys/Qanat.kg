{
  "name": "qanat-browser-test",
  "version": "1.0.0",
  "description": "Browser testing with Playwright",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
